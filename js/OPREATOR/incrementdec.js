/*let c = 10;
c = c++; //post increment opreator means it will first retun the value then it will increment
console.log(c);*/ //it will print 10 bcause before incrementing the value of which stored in 10 is printed

let c =10;
var d = c++; // return current value of c and then increment its own value it is equal to d = c;
console.log(d);//print 10 which is value 10 before incrementing
console.log(c); //print 11 after incrementing the value of c